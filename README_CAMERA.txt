CAMERA FIX
The camera code uses navigator.mediaDevices.getUserMedia().
For Chrome, run the project through VS Code Live Server (localhost) or HTTPS.
When prompted, choose Allow for Camera permission.
If permission was previously blocked, click the camera/lock icon in Chrome's address bar,
set Camera to Allow, reload the page, and click OPEN CAMERA.
